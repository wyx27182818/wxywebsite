# 技术设计：艺账

> Day 5 产出 · 撰写日期 2026-09-20
> 对应需求：PRD.md（v0.9.2 已实现全部 P0 功能）
> 本文回答两件事：**技术路线是什么、为什么选它**；**数据从哪来、到哪去**。

---

## 一、技术路线（一句话）

**纯前端单文件路线：一个 `index.html` 装下全部界面与逻辑，账目和截图存在浏览器本机的 IndexedDB 里，导出与备份用浏览器自带的文件能力直接写进你指定的文件夹——全程没有服务器，也没有数据库。**

拆开就是三行：

| 层 | 艺账的选择 | 一句话解释 |
|---|---|---|
| 前端 | 原生 HTML + CSS + JavaScript，**单个 index.html** | 界面与逻辑都在你眼前，双击即用 |
| 后端 | **没有后端** | 没有服务器参与，所以不存在“谁在服务器上能看到我的账” |
| 数据库 | 浏览器 **IndexedDB**（+ 文件夹里的 JSON 备份） | 数据存在你自己的电脑里，浏览器数据被清还有备份兜底 |
| 托管 | WorkBuddy 静态托管（原计划 GitHub Pages） | 只托管那一个文件，不接触任何账目数据 |

## 二、前端、后端、数据库的分工（为什么艺账只需要一块）

| 角色 | 通常干什么 | 类比 | 艺账里是谁 |
|---|---|---|---|
| **前端** | 画界面、接收你的操作、算数字、调浏览器的能力 | 店面：招呼客人、下单、结账 | ✅ 有（index.html） |
| **后端** | 保管数据、做权限校验、处理多个用户的请求 | 后厨 + 仓库管理员 | ❌ 没有 |
| **数据库** | 把数据长期存起来、支持多人同时读写 | 仓库货架 | ⚠️ 降级为「浏览器本地库」 |

结论：**只有“一个人、一台电脑”的场景，后端和数据库都可以不要。** 账目读写全在本机完成，所以速度最快、隐私最好、也没有服务器费用。代价是：换电脑看不到（Day 23 学数据库后再解决，这条在 PRD 里属于被砍掉的功能）。

## 三、选型表：每一项选了什么、为什么、代价是什么

| # | 决定 | 备选方案 | 为什么选它 | 代价 |
|---|---|---|---|---|
| 1 | 单文件纯前端 | 多文件工程 / Vue、React 框架 | 零安装、零构建，双击或开网址就用；一个文件能从头读到尾，出问题自己就能查 | 文件长到 1500 行左右，再复杂就该拆了 |
| 2 | 数据存 **IndexedDB** | localStorage / 云端数据库 | localStorage 只有约 5MB，装不下截图；IndexedDB 能存大容量二进制；云端违背「数据自持」 | 换设备看不到；浏览器数据可能被清理（已用备份兜底） |
| 3 | 截图**嵌入单元格**用 WPS 的 DISPIMG | 浮动图片 / 只写“见图片”文字 | 只有 DISPIMG 是真正“图在格子里”，满足你“必须在指定单元格”的硬要求 | 微软 Excel 打开显示 `#NAME?`（有意取舍，见 PRD 第五节） |
| 4 | 自己手写 Excel 两种格式 | 引第三方表格库 | 不引任何依赖，永远离线可用；.xls 用 SpreadsheetML、.xlsx 用自写 ZIP 打包 | 代码量大，但一次性写完就不用管了 |
| 5 | 固定导出文件夹（File System Access API） | 每次导出走浏览器“下载” | 文件固定落位才有“记录”作用，软件更新、清缓存都不影响 | 手机 Safari 不支持该 API → 自动回退成普通下载 |
| 6 | 备份用整体覆盖的 JSON | 版本历史 / 增量备份 | 单用户场景，只要“永远保留最新完整账本”就够 | 没有历史版本可回溯（够用，不做） |
| 7 | 主题用 CSS 变量 + `data-theme` | 写多套 CSS | 换主题 = 换一组变量值，布局代码一行不动 | 无 |
| 8 | 分类存「路径字符串」+ 前端建树 | 邻接表 / 嵌套对象 | 存储极简（`餐饮/水果`）；导出的 Excel 直接可读；不需要数据迁移 | 改一级名字时要批量改前缀（已实现） |
| 9 | 部署到静态托管 | 自己的服务器 | 免费、链接固定、随时更新 | 链接域名为平台提供（PRD 已砍掉自定义域名） |
| 10 | 版本管理用 Git + GitHub | 手动存副本 | 每次改动有记录、可回退（Day 2 起） | 需要遵守提交规范（已在做） |

## 四、数据流图：数据从哪来、到哪去

```
        【数据从哪来】
  手机支付截图 ──手动传到电脑──┐
  你手工填写的四个字段 ────────┤
  品类 / 时间 / 买什么 / 金额  │
                              ▼
              ① 录入表单（浏览器里的 index.html）
                              │  点「记一笔」保存
                              ▼
              ② IndexedDB：yizhang-db / entries
                 每条 = 品类路径 + 日期 + 物资 + 金额
                        + 截图（多张已自动拼成一张）
                 ── 只存在这台电脑的浏览器里 ──
                              │  打开页面时读取
                              ▼
              ③ 页面计算与展示（不出本机）
                 · 树形分类（父级小计含所有子分支）
                 · 时间段总额 + 笔数
                 · 明细列表（改名 / 归类 / 删除 / 看大图）
                              │
                    「导出」或「备份」——只有你点了才发生
                              ▼
        【数据到哪去：你选的固定导出文件夹】
          艺账_20260901-20260919.xls      ← 复刻《小艺资金表》格式
          艺账_20260901-20260919.xlsx     ← 截图嵌在单元格内
          艺账拼图_20260901-20260919.png  ← 时间段内截图拼成一张
          艺账备份.json                   ← 每次改动整体覆盖
```

**读图三句话：**

1. **进来的只有两样**：你手机上的截图（手工传进来），和你手填的四个字段。
2. **中间只有一站**：浏览器本机的 IndexedDB；页面上看到的所有数字都是现算的，不是从别处取的。
3. **出口只有你手点的那一下**：导出 / 拼长图 / 自动备份，落点是你选的固定文件夹。**没有任何一条线通向互联网。**

**数据项去向表：**

| 数据 | 从哪来 | 存在哪 | 到哪去 |
|---|---|---|---|
| 截图 | 手机支付截图（手工传） | IndexedDB（多张拼成一张后存） | .xlsx 的 E 列格内 / 拼长图 / 备份 JSON |
| 品类 | 手工输入（支持 `餐饮/水果` 分级） | IndexedDB（存路径字符串） | .xls/.xlsx 的 A 列（合并分组） |
| 时间 | 手工选（默认今天） | IndexedDB | B 列（真日期格式 `2026年9月12日`） |
| 买什么 | 手工输入，留空自动起名 | IndexedDB | C 列 |
| 金额 | 手工输入（必填） | IndexedDB | D 列 + F 列 SUM 合计 |
| 全部账目副本 | 由以上数据生成 | 固定文件夹的 `艺账备份.json` | 恢复时读回 IndexedDB |
| 主题偏好 / 折叠状态 | 页面里的选择 | localStorage | 只在本机生效，不入库 |

## 五、方案变化时，哪些文件会受影响（余力加练）

| 如果哪天要改… | 受影响的文件 | 是否要改 PRD |
|---|---|---|
| 加 OCR 自动读金额 | index.html（录入流程）+ 可能需要联网/模型 | 要（要从「砍掉清单」里移出来） |
| 加图表（趋势、占比） | index.html（新增渲染逻辑） | 要（PRD 明确砍掉了“除总额外的图表”） |
| 上云同步、换设备可见（Day 23） | 新增后端/数据库相关文件与 `.env`（永不入库）；index.html 的数据层要改 | 要（F 系列 + 验收标准要新增） |
| 导出格式再改（列序、样式） | index.html 的导出函数（`buildWorkbookXml` / `buildXlsx`） | 要（A5/A6 验收标准） |
| 加/换主题 | index.html 的 CSS 变量块（一处） | 不用（F11 已涵盖） |
| 改品类层级规则 | index.html（`normalizeCat` + 建树 + 改名逻辑） | 要（F2） |
| 换托管平台 / 绑域名 | 不涉及代码文件，只动发布设置 | 要（砍掉清单里的“自定义域名”） |
| 备份改成历史版本 | index.html 的 `backupEntries` | 要（F10 + A8） |

用法：改动前先在这张表里找一行——**知道会牵动哪些文件、要不要同步更新需求文档，就不会改出“文档和代码打架”的局面。**

## 六、附：开发网页会用到哪些技术（余力加练·技术栈是什么）

| 技术 | 是什么 / 干什么 | 优势 | 劣势 |
|---|---|---|---|
| **HTML** | 页面的骨架：有哪些元素（输入框、按钮、列表） | 浏览器原生支持、结构清晰 | 只能描述结构，不能算不能动 |
| **CSS** | 页面的样式：颜色、间距、圆角、阴影、动画 | 换皮肤/换主题只需改变量；性能好 | 复杂布局要经验（Flex/Grid） |
| **JavaScript** | 页面的行为：读写数据、算总数、生成 Excel | 浏览器直接跑，无需安装 | 大项目需框架与类型检查才不易乱 |
| 框架（React / Vue 等） | 把界面拆成组件、自动更新视图 | 大项目好维护 | 要构建工具，**会破坏“双击即用”** |
| 后端（Node / Python 等） | 处理登录、多人、数据库读写 | 能跨设备、能协作 | 要服务器与运维，**隐私与成本都上升** |
| 数据库（MySQL / 云数据库） | 长期保存数据、支持并发 | 数据集中、可查询 | 要联网、要账号，**单机场景是负担** |

**艺账用到的浏览器原生能力清单**：IndexedDB（存账目与截图）、FileReader（读入截图）、Canvas（拼图与缩放）、File System Access API（写入固定文件夹）、localStorage（主题与折叠记忆）、TextEncoder/DataView（手写 ZIP 打包 .xlsx）。

**本期的技术克制清单**：不引框架、不引打包工具、不引表格库、不写 TypeScript、不要后端、不建数据库——理由只有一个：**保持“双击 index.html 就能用、断网也能用、数据不出本机”这条底线。**

## 七、今天没做但迟早要定的技术决定

1. **云端存储方案**（Day 23）：选哪家数据库、账号怎么做、截图放哪 —— 决定后要同步改 PRD 的砍掉清单与验收标准。
2. **手机端体验**：目前页面在手机浏览器可打开，但固定文件夹与拼图体验受限；是否做适配待定（对应 PRD 砍掉的“手机原生 App”）。
3. **多账本 / 图表**：技术上都容易，但产品上已明确不做，除非需求变化。
