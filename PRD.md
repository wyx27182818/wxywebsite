# PRD：艺账 —— 自己的记账小工具

> Day 4 产出 · 撰写日期 2026-09-19
> 需求来源：Day 3《research.md》（研究定案）+ 本人实际使用确认
> 验收人：Joye（唯一用户）
> 实现状态：提前于计划，v0.9.2 已实现本文全部 P0 功能并上线（详见第八节）

---

## 一、背景与目标

手机付款、截图，攒到电脑上录进《小艺资金表》——现状是录入分散、截图散在文件夹里没法看、看总额只能手动改公式。

**艺账**要解决的是：让「记一笔账」的成本低到不会中断，并且账目留在自己手里。核心指标只有一个：**任意时间段内花了多少总额**，又准又好读。

一句话：**菜还是自己家做的放心**——数据只存在我自己的电脑浏览器里，无广告、无账号、无干扰。

## 二、目标用户与场景

- **用户**：只有我自己（Joye）。不考虑「别人打开好不好看」，只考虑「我愿不愿意天天打开它」。
- **场景**：手机付款 → 截图 → 传到电脑 → 打开艺账记一笔（挂截图）→ 随时按时间段看总额 → 定期导出 Excel 归档到《小艺资金表》同款格式。

## 三、用户故事

1. 作为记账的人，**我想在 30 秒内记完一笔**（品类可留空、金额必填、截图连加自动拼），这样记账不会中断。
2. 作为付了好几笔的人，**我想把同一笔花销的多张截图自动拼成一张**，这样一笔账只对应一张图。
3. 作为要看数的人，**我想按任意时间段一键看到总额和笔数**，不用再手动改 SUM 公式。
4. 作为爱归类的人，**我想让品类一层层往下分支**（餐饮 → 水果 → 进口水果），并让上级自动算出含所有下级的小计。
5. 作为要交账目的人，**我想导出和《小艺资金表》一模一样格式的 Excel**，截图直接嵌在单元格里，能直接交给别人看。
6. 作为怕丢数据的人，**我想让账目每次改动后自动备份到一个固定文件夹**，浏览器数据被清也能一键恢复。

## 四、功能需求

| 编号 | 功能 | 优先级 | 要求 |
|---|---|---|---|
| F1 | 记一笔 | P0 | 四字段：品类（可留空）、时间（默认今天）、购买物资（可留空自动起名）、金额（必填 > 0）；截图可挂多张 |
| F2 | 品类分支（无限层级） | P0 | 品类用 `/` 分级（如 `餐饮/水果`），列表渲染成可折叠的树；父级小计与笔数**包含所有子孙**；改某级名字时子级前缀跟随；归类/录入均支持写路径 |
| F3 | 截图自动拼图 | P0 | 多张截图（逐张「＋」加或多选）自动左右拼成一张存储；该笔永远只有一张图 |
| F4 | 时间段总额 | P0 | 快捷「今天 / 本月 / 全部」+ 自定义起止日期；显示总额与笔数；默认选中「本月」 |
| F5 | 列表管理 | P0 | 明细原地改名、归类到任意路径、两段式删除（先点变「确认删除」再点才删）、点缩略图看大图 |
| F6 | 导出 Excel（.xls） | P0 | SpreadsheetML 格式，一比一复刻《小艺资金表》`9月12日-` 页：A 品类（纵向合并、**不加「品类」表头**）/ B 时间（真日期，同日合并）/ C 购买物资 / D 金额（￥#,##0.00）/ E 截图（文字占位）/ F 总金额（=SUM，合并到末行）；无备注/水印列 |
| F7 | 导出 Excel 含截图（.xlsx） | P1 | 截图**嵌入 E 列单元格内**（WPS DISPIMG 机制），无浮动图片、无「见图片」文字；行高随截图自适应 |
| F8 | 拼长图 | P1 | 当前时间段内所有截图按时间顺序竖排成一张长图，每张带「品类 · 物资 · 日期 · 金额」标签条 |
| F9 | 固定导出文件夹 | P1 | File System Access API：选一次文件夹，之后导出与备份自动写入；不支持的浏览器自动回退普通下载 |
| F10 | 自动备份与恢复 | P1 | 每次记账/改名/删除后，把全部账目（含截图）静默写入 `艺账备份.json`（整体覆盖）；支持从备份一键恢复 |
| F11 | 三主题 | P2 | 白蓝款（默认）/ 黑白款 / 春天版；右上角低调小圆钮循环切换；切换平滑过渡；选择记忆，刷新不闪变 |

## 五、✂️ 砍掉的功能与理由（本期边界）

| 砍掉 | 为什么砍 |
|---|---|
| **OCR 识别截图金额** | 认真评估过：本地跑模型一张要 2–10 秒，达不到「1 秒以内」的底线；云端识别虽然 <1 秒，但截图要传给第三方服务器，违背「数据自持」的立身之本。手工填金额 5 秒能搞定，先保证流程顺。留作以后可选 |
| **收入 / 余额 / 账户** | 这是个人支出流水账，不是财务系统。加了收入就得有借贷逻辑，复杂度翻倍、使用频率极低 |
| **多账本 / 预算 / 复杂报表图表** | 随手记的病就在这儿：功能过载。我只看「时间段总额」这一个数 |
| **账号登录 / 多设备同步** | 需要服务器与数据库；单机浏览器存储已够用。等 Day 23 学数据库再做云端同步 |
| **报销流程字段**（谁付的、报销状态） | 已确认为个人花销，不存在报销场景 |
| **备注 / 水印字段** | 《小艺资金表》新格式（`9月12日-` 页）没有这两列；说明性信息由「名字」承担（见命名体系） |
| **微软 Excel 打开嵌图** | .xlsx 嵌图走 WPS 私有的 DISPIMG 机制——「图片真正嵌在单元格里」目前只有这条路径；代价是微软 Excel 打开会显示 `#NAME?`。我的电脑用 WPS，砍掉微软兼容，换「图必须钉在格子里」 |
| **手机原生 App** | 电脑浏览器打开网页即可满足全部流程；做 App 要开发者账号、上架审核，收益为零 |
| **广告 / 社区 / 理财推荐** | 永久不做。这是「记账 5 秒、关广告 10 秒」的反面 |
| **自定义域名** | 先用免费托管地址，域名不产生任何价值 |

## 六、非功能需求

1. **数据自持**：全部数据（含截图）只存在本机浏览器 IndexedDB；全程断网时记账、改、删、导出全部可用。
2. **无干扰**：页面无广告、无推广、无任何联网请求。
3. **零安装**：单文件 index.html，双击或打开网址即用，无需构建。
4. **容灾**：浏览器数据被清理时，可通过固定文件夹里的备份 JSON 一键找回全部账目。
5. **性能**：单笔录入到保存完成无感知延迟；拼图/导出在数秒内完成。

## 七、验收标准（可测，AI 自检即逐条核对）

| 编号 | 验收标准 |
|---|---|
| A1 | 金额留空或 ≤ 0 时无法保存并提示；品类留空保存后归入「未分类」；物资留空自动生成 `日期-序号`（如 `0919-1`），同日多笔序号递增 |
| A2 | 录入品类 `餐饮/水果` 后，列表出现「餐饮 → 水果」两级树；「餐饮」组头显示的笔数与小计**包含**「水果」下的账；收起「餐饮」后刷新页面，折叠状态保持 |
| A3 | 一笔账连加 ≥ 2 张截图后，存储与导出的都是自动拼好的一张图；.xlsx 中该行行高随图撑开、图完整显示 |
| A4 | 任意时间段（含自定义起止）的总额 = 该时段全部明细金额之和，与手工相加一致；打开页面默认选中「本月」 |
| A5 | 导出的 .xls 在 WPS/Excel 打开：列序、合并单元格、`2026年9月12日` 日期显示、`￥#,##0.00` 金额格式、F 列 SUM 合计全部与《小艺资金表》`9月12日-` 页一致；A 列第一行直接是品类名，没有「品类」表头 |
| A6 | 导出的 .xlsx 在 WPS 打开：每行 E 列的截图显示在**单元格内部**，不存在浮在表格上的图片，也不存在「见图片」文字 |
| A7 | 设置固定导出文件夹后，每次导出的文件与 `艺账备份.json` 自动出现在该文件夹内，无需再次选择 |
| A8 | 清空浏览器站点数据后，点「从备份恢复」，账目明细与截图全部还原 |
| A9 | 断开网络后，记账、改名、归类、删除、导出 .xls/.xlsx、拼长图全部正常；数据不出本机 |
| A10 | 三个主题点击切换时颜色平滑过渡（无生硬跳变）；刷新后保持上次选择；主题钮半透明低调但可发现 |

## 八、里程碑与现状

| 阶段 | 计划 | 实际 |
|---|---|---|
| Day 7 MVP | 录入 + 列表 + 时间段总额 | ✅ 已提前完成（v0.1，IndexedDB 存储） |
| 之后 | 截图、拼图、Excel、备份 | ✅ 已提前完成（v0.2–v0.6） |
| 增强 | 嵌图、拼图优化、主题、品类分支 | ✅ 已完成（v0.7–v0.9.2，2026-09-19） |
| Day 23 之后 | 数据库：云端保存、多设备同步 | 未开始（对应被砍的「多设备同步」） |

## 九、命名体系与格式基准（硬约定）

- **品类**留空 → 默认「未分类」；**截图小名字**留空 → 自动 `日期-序号`。名字可空、可改、可重名，绝不因「没想好名字」挡住记账。
- Excel 格式一律以《小艺资金表》`9月12日-` 页为准；旧页 `-9月11` 不采用，其整行红色是填错临时标记、不是格式约定。
- A 列不加「品类」表头；不设备注/水印字段。

## 十、风险与已知限制

1. .xlsx 嵌图仅 WPS 支持，微软 Excel 显示 `#NAME?`（有意取舍，见第五节）。
2. 浏览器 IndexedDB 可能被系统清理 → 已用固定文件夹自动备份兜底（F10 / A8）。
3. 拼长图受浏览器画布高度上限约束（约 3 万像素），超长需缩小时间段分批拼，导出时会给提示。
